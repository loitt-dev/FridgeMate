name: navigation
description: Navigation layer for FridgeMate - Routes and navigation
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.0.0
  domain:
    path: ../../core/domain      # For auth check
  
  # ❌ NO get_it

dev_dependencies:
  build_runner: ^2.4.0