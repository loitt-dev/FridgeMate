name: design_system
description: Design system for FridgeMate - Theme, styles, and components
version: 1.0.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  
  # ❌ NO domain/data dependencies
  # ❌ NO get_it

dev_dependencies:
  build_runner: ^2.4.0