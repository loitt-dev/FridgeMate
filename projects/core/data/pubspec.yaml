name: data
description: Data layer for FridgeMate - Repository implementations
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.9.2

dependencies:
  domain:
    path: ../domain              # Implement interfaces
  network:
    path: ../network             # Use Dio
  local_storage:
    path: ../local_storage       # Use Database
  
  dartz: ^0.10.1
  freezed_annotation: ^2.4.0
  json_annotation: ^4.8.0
  
  # ❌ NO get_it!

dev_dependencies:
  build_runner: ^2.4.0
  freezed: ^2.4.0
  json_serializable: ^6.7.0